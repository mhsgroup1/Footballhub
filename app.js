const ready=window.SUPABASE_URL&&window.SUPABASE_URL.startsWith('http')&&window.SUPABASE_PUBLISHABLE_KEY&&window.SUPABASE_PUBLISHABLE_KEY!=='YOUR_SUPABASE_PUBLISHABLE_KEY';
const sb=ready?supabase.createClient(window.SUPABASE_URL,window.SUPABASE_PUBLISHABLE_KEY):null;
const newsGrid=document.querySelector('#newsGrid'), videoGrid=document.querySelector('#videoGrid'), matchGrid=document.querySelector('#matchGrid');
function fallback(){videoGrid.innerHTML=['Highlights','Top Goals','Match Preview'].map(x=>`<article class="card"><div class="thumb">▶️</div><div class="pad"><span class="tag">Video</span><h3>${x}</h3><p>Add videos from your admin workflow.</p></div></article>`).join('');matchGrid.innerHTML=['Premier League','La Liga','Champions League'].map(x=>`<div class="match"><span class="tag">${x}</span><div class="teams"><span>Home</span><span class="score">VS</span><span>Away</span></div><p>Connect Supabase to load live fixtures.</p></div>`).join('')}
async function loadNews(){if(!sb){fallback();return}const {data,error}=await sb.from('news').select('*').eq('published',true).order('published_at',{ascending:false}).limit(12);if(error){console.error(error);fallback();return}newsGrid.innerHTML=data?.length?data.map(n=>`<article class="card"><div class="thumb" style="background-image:url('${n.image_url||''}');background-size:cover">${n.image_url?'':'⚽'}</div><div class="pad"><span class="tag">${n.category||'Football'}</span><h3>${n.title}</h3><p>${n.summary||''}</p></div></article>`).join(''):'<p>No published news yet.</p>';fallback()}
document.querySelector('#refreshNews').onclick=loadNews;document.querySelector('#loginBtn').onclick=async()=>{if(!sb)return document.querySelector('#authMsg').textContent='Add Supabase config in js/config.js first.';const email=document.querySelector('#email').value,password=document.querySelector('#password').value;const {error}=await sb.auth.signInWithPassword({email,password});document.querySelector('#authMsg').textContent=error?error.message:'Logged in! Open admin.html.';if(!error)location.href='admin.html'};loadNews();
